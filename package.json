{
  "name": "dockerizathinginator-tests",
  "version": "1.0.0",
  "type": "module",
  "description": "Testing for Dockerizathinginator navigation",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:simple": "playwright test test-navigation.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}